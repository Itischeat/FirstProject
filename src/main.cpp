#include <Arduino.h>

// uint32_t timer;
// uint32_t inter;

// uint32_t getInterval();

void setup() {
  Serial.begin(9600);
  Serial.available();
  // pinMode(13, OUTPUT);
  // inter = 100;
};

void loop() {

  // Serial.println(inter);
  // if (getInterval() >= inter && inter != 0) {
  //   timer = millis();
  //   digitalWrite(13, HIGH);
  //   delay(35);
  // };

  // digitalWrite(13, LOW);
};

// uint32_t getInterval() {
//     return millis() - timer;
// };

